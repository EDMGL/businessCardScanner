{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && pip install -r requirements.txt && python3 -m spacy download en_core_web_sm && python3 -m spacy download tr_core_news_sm"
  },
  "deploy": {
    "startCommand": "node ocrServer.js",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 